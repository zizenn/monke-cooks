#include "ui.hpp"

namespace ui {
      void Button::Draw() {
            // add the draw function which draws a texture
      }

      bool Button::DetectUpdate() {
            return false;
      }
};
